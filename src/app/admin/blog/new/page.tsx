'use client';

import BlogPostForm from '@/components/blog/BlogPostForm';

export default function AddBlogPost() {
  // Simply render the form component with no initial data
  return <BlogPostForm />;
}
